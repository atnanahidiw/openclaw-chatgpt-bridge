apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bridge
  namespace: openclaw-bridge
spec:
  rules:
    - host: bridge.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bridge
                port:
                  number: 80

