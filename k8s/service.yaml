apiVersion: v1
kind: Service
metadata:
  name: bridge
  namespace: openclaw-bridge
spec:
  selector:
    app: bridge
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

