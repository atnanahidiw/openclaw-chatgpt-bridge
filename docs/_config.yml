title: OpenClaw ChatGPT Bridge
description: Documentation for deploying and using the OpenClaw ChatGPT Bridge.

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

defaults:
  - scope:
      path: ""
    values:
      layout: default

# Attribution note for the logo files; not a site page.
exclude:
  - assets/img/README.md
