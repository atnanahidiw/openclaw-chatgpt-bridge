apiVersion: v1
kind: Service
metadata:
  name: {{ include "openclaw-chatgpt-bridge.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "openclaw-chatgpt-bridge.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080

