apiVersion: v1
kind: Secret
metadata:
  name: {{ include "openclaw-chatgpt-bridge.fullname" . }}-secret
type: Opaque
stringData:
  OPENCLAW_WEBHOOK_SECRET: {{ .Values.secrets.openclawWebhookSecret | quote }}

